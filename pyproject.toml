[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "the_schnitz"
version = "0.0.1"

dependencies = [
  "Flask",
  "click",
  "msgpack",
  "pika",
  "playsound3",
]

[project.scripts]
schnitz = "the_schnitz.cli:cli"
